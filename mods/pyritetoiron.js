elements.my_new_fluid = {
    color: "#0000ff", // Set the color (blue in this case)
    behavior: behaviors.LIQUID, // Set the behavior to LIQUID
    category: "Liquids", // Assign it to the Liquids category
    state: "liquid", // Specify its state as liquid
    density: 1000, // Set the density (adjust as needed)
    viscosity: 1, // Set the viscosity (higher = slower flow)
    // Add other properties like reactions, temperature effects, etc.
};
